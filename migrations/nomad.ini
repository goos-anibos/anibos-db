[nomad]
engine = sqla
url = postgresql://${DB_USER}:${DB_PASS}@/${ANIBOS_DATABASE_NAME}?host=/var/run/postgresql

[anibos]
host = localhost
user = anibos
pass = anibospass
port = 5432
database = anibos

